import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fazweb.app',
  appName: 'appmobil',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
